import numpy as np
import matplotlib.pyplot as plt
from matplotlib import cm, colors
from tqdm import tqdm
import re

plt.scatter([i for i in range(10)] ,[np.random.random() for i in range(10)])
plt.show()